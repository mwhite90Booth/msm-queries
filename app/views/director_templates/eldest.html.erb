<h1>Eldest director</h1>

<p>The eldest director in our dataset right now is <a href="/directors/<%=@oldest_director.id%>"><%=@oldest_director.name%></a>, born on <%=@oldest_director.dob.strftime('%b %d, %Y')%>.</p>

<p>(<a href="(http://strftime.net/ is handy for putting together the formatting strings for the .strftime method.)">http://strftime.net/</a> is handy for putting together the formatting strings for the .strftime method.)</p>
